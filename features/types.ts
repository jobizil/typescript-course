// Type is an easy way to refer to the different properties  & functions(methods) a value has.
// BTW, Types are used everywhere.

const today = new Date()
// e.g : today.getDate()
const person = { age: 20 }
class Color {}
const red = new Color()
